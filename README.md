<h1>Wolfbot - это телеграмм бот-модератор с собственной экономикой</h1>

<p>В связи с добавлением дополнительной библиотеки telethon пришлось убрать main.py и заменить на tele.py, чтоб всё работало отлично.<br><b>ВАЖНО:</b> запускать файл apitelegram.py и tele.py надо отдельно(т.е использовать два терминала в vs code. Иного решения пока не увидел) ибо две библиотеки пытаются перехватить инициативу при попытке запустить в одном терминале. При запуске файлов нужно будет указать код, который заранее прописан в файлах. Это некоторая мера защиты от активации другого файла</p>
